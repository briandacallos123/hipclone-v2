// sections
import { ManageProfile } from "@/sections/manage/@view";
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Manage Profile',
};

export default function ManageProfileAll() {
  return <ManageProfile />;
}
