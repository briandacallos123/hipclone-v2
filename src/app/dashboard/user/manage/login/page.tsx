// sections
import { UserLoginView } from '@/sections/user/@view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Manage Log-in',
};

export default function UserLoginPage() {
  return <UserLoginView />;
}
