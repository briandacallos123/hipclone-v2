// sections
import { OverviewTodayListView } from 'src/sections/overview/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: `Overview: Today's Clinic`,
};

export default function OverviewTodayListPage() {
  return <OverviewTodayListView />;
}
