// sections
import { HomeView } from '@/sections/home/@view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'HIP: The starting point for your HIP experience',
};

export default function HomePage() {
  return <HomeView />;
}
